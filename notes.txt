
Canvas
WebGLRenderer() - ustala canvas do THREE
setsize(w ,h)

Camera - perspective or ortohraphic (field of view, ratio w/h, 0.1)
Scena - Tworzy miejsce gdzie można dodawać elementy
Renderer - Tworzy Canvas na stronie, w którym będą się wyświetlać elementy 


geometry - ustala kształ figury 
const geometry_gwint = new THREE.CylinderGeometry( 7.5, 7.5, 5, 32 ); 

material - możliwość zmienienia właściwości i wyglądu
const material_gwint = new THREE.MeshBasicMaterial( {color: 0xffff00} ); 

mesh - generuje model po połączeniu kształtu i materiału
const cylinder2 = new THREE.Mesh( geometry_gwint, material_gwint ); scene.add( cylinder2 );
cylinder2.position.set(0, 10, 0)